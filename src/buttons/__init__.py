"""GPIO button handlers for BatGlass.

Button assignments:
    GPIO 17 — scene description (Gemini)
    GPIO 27 — OCR (Gemini text reading)
    GPIO 22 — beep (proximity feedback)
    GPIO 5 — volume up
    GPIO 6 — volume down
"""
