"""GPIO button handlers for BatGlass.

Button assignments:
    GPIO 17 — scene description (VLM)
    GPIO 27 — OCR (read text aloud)
    GPIO 22 — beep (proximity feedback)
    GPIO 5 — volume up
    GPIO 6 — volume down
"""
